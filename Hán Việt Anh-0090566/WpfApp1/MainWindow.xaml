<Window x:Class="BTCTColumnDesign.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Thiết kế Cột BTCT - Chịu kéo nén lệch tâm"
        Height="600" Width="900" WindowStartupLocation="CenterScreen"
        Background="#FFF7F7F7">
    <Grid Margin="8">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Window.Background>
            <ImageBrush ImageSource="/Resources/Image/anhcot.jpg" 
                    Stretch="UniformToFill" />
        </Window.Background>
        <!-- Thông số cơ bản -->
        <GroupBox Header="Thông số cơ bản" Margin="4" Padding="6">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="200"/>
                    <ColumnDefinition Width="200"/>
                    <ColumnDefinition Width="200"/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <TextBlock Text="Chiều rộng B (m):" Grid.Row="0" Grid.Column="0" Margin="4"/>
                <TextBox x:Name="TxtB" Grid.Row="0" Grid.Column="1" Margin="4" Text="0.40"/>

                <TextBlock Text="Chiều cao H (m):" Grid.Row="1" Grid.Column="0" Margin="4"/>
                <TextBox x:Name="TxtH" Grid.Row="1" Grid.Column="1" Margin="4" Text="0.60"/>

                <TextBlock Text="Cấp bền Bê tông:" Grid.Row="2" Grid.Column="0" Margin="4"/>
                <ComboBox x:Name="CmbConcrete" Grid.Row="2" Grid.Column="1" Margin="4" SelectedIndex="0">
                    <ComboBoxItem Content="B15"/>
                    <ComboBoxItem Content="B20"/>
                    <ComboBoxItem Content="B25"/>
                </ComboBox>

                <TextBlock Text="Thép dọc:" Grid.Row="3" Grid.Column="0" Margin="4"/>
                <ComboBox x:Name="CmbSteel" Grid.Row="3" Grid.Column="1" Margin="4" SelectedIndex="0">
                    <ComboBoxItem Content="AI"/>
                    <ComboBoxItem Content="AII"/>
                    <ComboBoxItem Content="AIII"/>
                </ComboBox>
            </Grid>
        </GroupBox>

        <!-- Khu vực tính toán và hình minh họa -->
        <Grid Grid.Row="1" Margin="4">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="2*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- Bảng kết quả -->
            <DataGrid x:Name="ResultGrid" Grid.Column="0" Margin="4" AutoGenerateColumns="False" HeadersVisibility="Column" CanUserAddRows="False">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="TT" Binding="{Binding TT}" Width="40"/>
                    <DataGridTextColumn Header="B (m)" Binding="{Binding B}" Width="60"/>
                    <DataGridTextColumn Header="H (m)" Binding="{Binding H}" Width="60"/>
                    <DataGridTextColumn Header="Cấp BT" Binding="{Binding Concrete}" Width="80"/>
                    <DataGridTextColumn Header="Thép" Binding="{Binding Steel}" Width="80"/>
                    <DataGridTextColumn Header="As (cm2)" Binding="{Binding As}" Width="80"/>
                    <DataGridTextColumn Header="Md (kNm)" Binding="{Binding Md}" Width="100"/>
                    <DataGridTextColumn Header="Nd (kN)" Binding="{Binding Nd}" Width="100"/>
                </DataGrid.Columns>
            </DataGrid>

            <!-- Hình minh họa -->
            <Border Grid.Column="1" Background="White" BorderBrush="Gray" BorderThickness="1" Margin="4" Padding="8">
                <StackPanel>
                    <TextBlock Text="Minh họa tiết diện" FontWeight="Bold" Margin="0,0,0,6"/>
                    <Canvas Width="200" Height="200" Background="#FFF9F9F9" x:Name="CanvasSection"/>
                </StackPanel>
            </Border>
        </Grid>

        <!-- Nút chức năng -->
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="4">
            <Button Content="Thêm" Click="BtnAdd_Click" Width="80" Margin="4"/>
            <Button Content="Xóa" Click="BtnDelete_Click" Width="80" Margin="4"/>
            <Button Content="Tính toán" Click="BtnCompute_Click" Width="100" Margin="4"/>
            <Button Content="Báo cáo" Width="100" Margin="4"/>
            <Button Content="Kết thúc" Click="BtnExit_Click" Width="100" Margin="4"/>
        </StackPanel>
    </Grid>
</Window>
